apiVersion: v1
kind: Service
metadata:
  name: express-svc
  namespace: express-ns
  labels:
    app: express-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  selector:
    app: express