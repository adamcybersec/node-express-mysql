apiVersion: v1
kind: Service
metadata:
  name: express-svc
  namespace: express-app-ns
  labels:
    app: express-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
  selector:
    app: express